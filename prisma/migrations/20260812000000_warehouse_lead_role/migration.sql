-- Согласовано с владельцем 2026-08-12 — роль «Зав. складом».
--
-- Два уровня складского персонала:
--   WAREHOUSE       — складчик: размеры, дата, поставщик, узоры, локации;
--   WAREHOUSE_LEAD  — зав. складом: то же + КОЛИЧЕСТВО (плиты/м²).
--
-- Количество остаётся чувствительной операцией: оно влияет на остаток, брони,
-- долги и продажи. Поэтому им владеют только владелец и зав. складом.
--
-- Права по существующим пользователям НЕ меняются: новых WAREHOUSE_LEAD нет,
-- владелец назначает их вручную в «Сотрудниках».

ALTER TYPE "Role" ADD VALUE IF NOT EXISTS 'WAREHOUSE_LEAD';
